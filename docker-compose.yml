services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    environment:
      HOST: 0.0.0.0
      NODE_ENV: production
      PORT: 3000
    ports:
      - '${APP_PORT:-3000}:3000'
